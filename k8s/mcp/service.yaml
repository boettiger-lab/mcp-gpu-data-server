apiVersion: v1
kind: Service
metadata:
  name: mcp-server-gpu
  namespace: mcp
spec:
  type: ClusterIP
  selector:
    app: gpu-mcp
  ports:
    - protocol: TCP
      port: 8000
      targetPort: http
      name: http
